import React from 'react';

//will display single cycle
// pull from cycle collection

export default React.createClass({
  render() {
    return (
      <li className="cycle-preview">
        <span className="cycle-name"> Strength </span>
      </li>
    );
  }
});
